import { makeStyles } from '@material-ui/core';

export const useMealTextRepresentationStyles = makeStyles({
  container: {},
  textarea: {
    resize: 'none',
    width: '100%',
    minHeight: 300,
  },
});
