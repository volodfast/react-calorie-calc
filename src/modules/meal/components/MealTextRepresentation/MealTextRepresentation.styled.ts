import { makeStyles } from '@material-ui/core';

export const useMealTextRepresentationStyles = makeStyles({
  container: {
    textAlign: 'center',
  },
  copyButton: {
    margin: '5px 10px',
  },
  textarea: {
    resize: 'none',
    width: '100%',
    minHeight: 100,
  },
});
